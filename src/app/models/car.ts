export interface Car {
    id: string;
      carCategory: string;
      fuelSource: string;
      transmission: string;
      make: string;
      model: string;
      modelYear: number;
      doors: number;
      color: string;
      mileage: number;
      imageTitle: string;
}